# Chatbot Frontend

Run locally:

```bash
cd chatbot/frontend
npm install
npm run dev
```

The dev server proxies `/api` to `http://127.0.0.1:8090` by default.

Main features:

- executive analyst chat experience
- starter prompts
- streaming status updates
- SQL visibility and trust panel
- chart and table rendering for demo-friendly result shapes
